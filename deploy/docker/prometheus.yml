global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: hookmon-agent
    static_configs:
      - targets: ["host.docker.internal:2112"]
        labels:
          instance: hookmon-agent
